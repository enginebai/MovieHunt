<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context="com.moviebomber.ui.activity.CommentActivity">

    <include
        layout="@layout/toolbar"
        android:id="@+id/toolbar"/>
    <it.neokree.materialtabs.MaterialTabHost
        android:id="@+id/tab_comment_list"
        android:layout_height="48dp"
        android:layout_width="match_parent"
        app:textColor="@color/text"
        app:primaryColor="?attr/colorPrimary"
        app:accentColor="@android:color/white"
        android:layout_below="@id/toolbar"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/pager_comment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tab_comment_list"/>
</RelativeLayout>
